# Cloud Build configuration for DEVELOPMENT environment
# Triggered on commits to 'dev' branch

steps:
  # Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.backend'
      - '-t'
      - 'gcr.io/$PROJECT_ID/synapse-backend-dev:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/synapse-backend-dev:latest'
      - '.'

  # Push images
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/synapse-backend-dev:$COMMIT_SHA']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/synapse-backend-dev:latest']

  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'synapse-backend-dev'
      - '--image=gcr.io/$PROJECT_ID/synapse-backend-dev:$COMMIT_SHA'
      - '--region=asia-south1'
      - '--platform=managed'
      - '--allow-unauthenticated'
      - '--max-instances=10'
      - '--min-instances=0'
      - '--memory=1Gi'
      - '--cpu=1'
      - '--timeout=300'
      - '--concurrency=80'
      - '--set-env-vars=ENVIRONMENT=development,API_V1_STR=/api/v1,CLOUD_SQL_CONNECTION_NAME=synapse-473918:asia-south1:synapse,STORAGE_BACKEND=gcs,GCS_BUCKET_NAME=synapse_storage,GCS_PROJECT_ID=$PROJECT_ID,DATABASE_URL=postgresql+asyncpg://dev_user:YOUR_PASSWORD@/dev,MAX_CONTENT_SIZE_MB=50,CHUNK_SIZE=500,CHUNK_OVERLAP=50,SIMILARITY_THRESHOLD=0.7,EMBEDDING_MODEL=text-embedding-ada-002,CHAT_MODEL=gpt-4o-mini,MAX_CHAT_HISTORY=10,CHAT_TIMEOUT_SECONDS=60,RATE_LIMIT_PER_MINUTE=60,LOG_LEVEL=INFO,ENABLE_CACHING=true,CACHE_TTL_SECONDS=3600'
      - '--set-secrets=SECRET_KEY=dev-secret-key:latest,OPENAI_API_KEY=dev-openai-key:latest'
      - '--add-cloudsql-instances=synapse-473918:asia-south1:synapse'

images:
  - 'gcr.io/$PROJECT_ID/synapse-backend-dev:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/synapse-backend-dev:latest'

timeout: 1200s

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'N1_HIGHCPU_8'
