# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic>=2.5.0
pydantic-core>=2.14.5
pydantic-settings>=2.3.0  # Updated for docling compatibility (was 2.1.0)
email-validator==2.1.0

# Database
sqlalchemy==2.0.23
alembic==1.13.0
asyncpg==0.29.0

# Authentication & Security
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
cryptography==41.0.7

# HTTP client
httpx==0.24.1

# Storage backends
boto3==1.34.0  # AWS S3
google-cloud-storage==2.10.0  # Google Cloud Storage
google-cloud-iam==2.15.0  # IAM for signed URL generation on Cloud Run
supabase==2.3.0  # Supabase client

# Background tasks
celery==5.3.4
redis==5.0.1

# Text processing
PyMuPDF==1.26.4  # Best PDF library - fast & accurate (fitz)
pdfplumber==0.11.0  # Fallback for tables/complex layouts
python-docx>=1.1.2  # Updated for docling compatibility (was 0.8.11)
openpyxl>=3.1.0  # Excel preprocessing to extract values instead of formulas
beautifulsoup4>=4.12.3  # Updated for docling compatibility (was 4.12.2)
python-magic==0.4.27
chardet==5.2.0  # Encoding detection for text files
# OCR for scanned PDFs and images (requires system tesseract: brew install tesseract)
pytesseract==0.3.13  # Fallback OCR engine
pillow>=10.0.0  # Required for image processing, OCR, and docling (flexible for compatibility)

# AI-powered document processing
docling>=2.55.0  # Unified document processing (PDF, DOCX, PPTX, XLSX, HTML)
docling-core[chunking-openai]>=2.8.0  # Smart chunking with structure-awareness and token limits
easyocr>=1.7.0  # Advanced OCR engine (80+ languages, GPU support)
# torch>=2.0.0  # Installed separately with CPU-only version in Dockerfile
transformers>=4.37.0  # HuggingFace models needed by Docling's AutoProcessor
tiktoken>=0.5.0  # OpenAI tokenizer for token-aware chunking

# Vector database
pgvector==0.2.4

# Monitoring & Logging
structlog==23.2.0
prometheus-client==0.19.0
sentry-sdk==1.39.1

# Production server
gunicorn==21.2.0

# Development utilities
python-dotenv==1.0.0

# Testing (dev only)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Development tools (dev only)
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Documentation (dev only)
mkdocs==1.5.3
mkdocs-material==9.4.8
