1. all texts shwoing Currently all the texts in the knowledge base on the item are showing not processable even though they have been processed in the backend and the chunks are created. Create a plan on how to do this or is there a database table where we can pull when we click on the item ID or when we open the folder or the page once we pull it and get all the status of all the text. The actual status is whether they have been processed into vectorized format or not and whether they are searchable or not. In the content we need to show that the item is still under process if the item has not been processed. The second issue is if I upload a file that is of 35 MB somehow it is showing that field to upload files is there a limit on these files and if yes, can we remove the limit on the file size that we are uploading. The third issue is we need to support multiple data types such as docs, ppt and we need to cover most of the document files. Find a way on how we can do this. We can use different libraries but is there a way we can reliably and more effectively and efficiently use some libraries to extract data from it. I want to keep the Docker image as small as possible but even if we are compromising the quality of text, I don't want to do it. I want to use specific libraries if needed.
For the mobile version, on the knowledge base, can we reduce the amount of data that we are showing for each item? It still looks a bit congested and I want to make it simple for the user to understand what is the knowledge item and when he clicks on it, probably we can show more information about the item ID. But just looking at it, it's a bit congested. Also on the knowledge base, on the desktop application, if I click upload and the file size is very very big, the dialog box gets stretched a lot and the whole UI gets disturbed. Can we find a way to crop it or somehow see that within the dialog box itself?